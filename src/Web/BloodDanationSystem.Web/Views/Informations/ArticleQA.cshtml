@{
    ViewData["Title"] = "Article";
}

<div class="card">
    <div class="card-header">
    </div>
    <div class="card-body">
        <p class="card-text">@Html.Raw(ViewBag.WordHtml)</p>
    </div>
</div>


@{
    ViewData["Title"] = "MyDonors";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@if (this.ViewData["Photo"] != null)
{
    <div class="donors-image">
        <a href="@this.ViewData["Photo"]" download>
            <img src="@this.ViewData["Photo"]">
        </a>
    </div>
}
else
{
    <div class="become_volunter volunter_bg_1">
        <div class="container">
            <div class="row">
                <div class="col-xl-8 offset-xl-2 col-md-10 offset-md-1">
                    <div class="volunter_text text-center">
                        <h3>Няма служебни бележки, които да изтеглите</h3>
                    </div>
                </div>
            </div>
        </div>
    </div>
}
